name: Checks

on:
  push:
    branches: [ master ]
  pull_request:

jobs:
  tests:
    name: Checks
    runs-on: ubuntu18.04
    steps:
      - uses: actions/checkout@v2
      - name: Git version
        run: |
          git --version
      - name: Run check
        run: |
          make check
